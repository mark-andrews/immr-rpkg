% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binomial_model.R
\name{binomial_model}
\alias{binomial_model}
\title{Fit a binomial model}
\usage{
binomial_model(
  successes,
  trials,
  group = NULL,
  data = NULL,
  multilevel = FALSE,
  conf_level = 0.95
)
}
\arguments{
\item{successes}{<\code{\link[rlang:args_data_masking]{data-masking}}> Number of
successes. Unquoted column name when \code{data} is supplied, or a numeric
vector.}

\item{trials}{<\code{\link[rlang:args_data_masking]{data-masking}}> Number of trials.}

\item{group}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional grouping
variable. When omitted, a single pooled probability is estimated.}

\item{data}{A data frame.}

\item{multilevel}{Logical. When \code{TRUE} and \code{group} is supplied, fits a
multilevel model with normally distributed random intercepts via
\code{\link[lme4:glmer]{lme4::glmer()}}. Default \code{FALSE}.}

\item{conf_level}{Confidence level. Default \code{0.95}.}
}
\value{
An object of class \code{binomial_model}. Components vary by model type:

\strong{Pooled} (no \code{group}):
\itemize{
\item \verb{$estimate} — probability estimate
\item \verb{$conf_int} — named vector \code{c(lower, upper)} on the probability scale
}

\strong{Unpooled} (\code{group}, \code{multilevel = FALSE}):
\itemize{
\item \verb{$estimates} — tibble with columns \code{group}, \code{estimate}, \code{lower}, \code{upper}
}

\strong{Multilevel} (\code{group}, \code{multilevel = TRUE}):
\itemize{
\item \verb{$estimate} — grand mean probability (fixed effect, probability scale)
\item \verb{$conf_int} — CI on the grand mean
\item \verb{$prediction_int} — predictive interval for a new group
(grand mean ± z·τ, probability scale)
\item \verb{$tau} — random-effects SD on the log-odds scale
\item \verb{$estimates} — tibble of partial-pooled per-group probabilities
\item \verb{$random_effects} — tibble of group-level deviations (log-odds scale)
}

All types also store the underlying model object as \verb{$model}.
}
\description{
Estimates the probability of success from binomial count data.
Handles a single pooled estimate, unpooled per-group estimates, and a
multilevel (partial pooling) model, without requiring the caller to deal
with \code{cbind}, \code{family = binomial()}, log-odds transforms, or the underlying
model objects.
}
\examples{
# Single group
M1 <- binomial_model(m, n, data = dplyr::filter(rats, batch == 42))
M1$estimate
M1$conf_int

# All groups pooled into one estimate
M2 <- binomial_model(m, n, data = rats)

# Unpooled: separate estimate per group
M3 <- binomial_model(m, n, group = batch, data = rats)
M3$estimates

# Multilevel: partial pooling across groups
M4 <- binomial_model(m, n, group = batch, data = rats, multilevel = TRUE)
M4$estimate
M4$conf_int
M4$prediction_int
M4$tau
M4$estimates

}
